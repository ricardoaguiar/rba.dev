import styled from "@emotion/styled"

export const GitHubButton = styled.button`
display: flex;
  border: none;
  cursor: pointer;
  padding: 0;
  z-index: 10;
  grid-area: 1/3/1/-1;
  margin: 0 auto;
  align-self: center;
  z-index: 10;
  background: transparent;
`
